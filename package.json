{
  "name": "gauge-challenge",
  "private": true,
  "version": "2.0.0",
  "workspaces": ["demo"],
  "scripts": {
    "dev": "npm run dev --workspace demo",
    "build": "npm run build --workspace demo",
    "check": "npm run check --workspace demo",
    "serve": "npm run serve --workspace demo"
  }
}
